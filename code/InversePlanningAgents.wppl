var actionPrior = Categorical({vs: [60, 70, 80], ps: [1/3, 1/3, 1/3]})
var PriceSetting = function (state, action) {
	return (action == 60 ? categorical ({vs: ["unresponsive", "self-serving", "cooperative"], ps: []})) :
	
}